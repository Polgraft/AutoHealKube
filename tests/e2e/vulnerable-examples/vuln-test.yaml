# tests/e2e/vulnerable-examples/vuln-test.yaml - Vulnerable manifest do testów (bez securityContext dla Kyverno mutate/block)
# WARNING: Tylko do testów!
# Cel: Pod bez non-root – Kyverno powinien mutate, Falco detect runtime.

apiVersion: apps/v1
kind: Deployment
metadata:
  name: vuln-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vuln-test
  template:
    metadata:
      labels:
        app: vuln-test
    spec:
      containers:
      - name: nginx
        image: your-vuln-nginx:latest  # Zbuduj z Dockerfile
        # Brak securityContext – celowo vuln